
<Card class="mt-5 ">
    <div class="card m-auto p-5 mt-5 bg-dark shadow-lg mt-5">
        <h1 class="text-warning text-center">Add Product</h1>
        <div class="d-flex mt-4 justify-content-lg-evenly">
            <input type="text" placeholder="Enter Product Name " id="Product" class="mb-3 me-3 bg-dark border-warning text-warning">
            @*                        <input type="password" name="" placeholder="Select Image " id="" class="mb-3 bg-dark border-warning text-warning">
            *@
        </div>
        <div class="d-flex justify-content-lg-evenly">
            <input type="text" placeholder="Product Type" id="ProductType" class="mb-3 me-3 bg-dark border-warning text-warning">
            @*                        <input type="password" name="" placeholder="Product Id" id="ProductId" class="mb-3 bg-dark border-warning text-warning">
            *@
        </div>
        <div class="d-flex mt-4 justify-content-lg-evenly">
            <input type="text" placeholder="Enter Quality " id="ProductQuality" class="mb-3 me-3 bg-dark border-warning text-warning">
            <input type="password" name="" placeholder="Product Quantity" id="ProductQuantity" class="mb-3 bg-dark border-warning text-warning">
        </div>
        <div class="d-flex justify-content-lg-evenly">
            <input type="text" placeholder="Product Color" id="ProductColor" class="mb-3 me-3 bg-dark border-warning text-warning">
            <input type="text" placeholder="Product Price" id="ProductPrice" class="mb-3 me-3 bg-dark border-warning text-warning">
        </div>
        <div class="d-flex justify-content-lg-evenly">
            <input type="text" placeholder="Product Description" id="ProductDes" class="mb-3 me-3 bg-dark border-warning text-warning">
            @*                        <button type="submit" id="btnadd">AddPrdouct</button>
            *@
        </div>
        <div class="d-flex justify-content-lg-evenly">
            <input type="file" id="formFile" name="formFile" />
            <input type="submit" value="Upload" />
        </div>
    </div>
</Card>
</div>

}
<script>
    $('#btnadd').click(function () {
        var Product = $('Product').val();
        var Product = $('ProductImage').val();
        var Product = $('ProductQuality').val();
        var Product = $('ProductType').val();
        var Product = $('ProductId').val();
        var Product = $('ProductColor').val();
        var Product = $('ProductPrice').val();
        var Product = $('ProductDes').val();
        $.ajax({
            url: '/Home/AddProducts'
                type: 'Post',
            datatype: 'JSON',
            data: {

            }, success: function (data) {
                alert("Product has been addedsuccessfully");
            },
            error: function(data){
                alert("Error");
            }
            })
    })
</script>